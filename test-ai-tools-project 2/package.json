{
  "name": "gstack-phase3-api",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate:dev": "prisma migrate dev --name init --skip-seed",
    "test": "echo \"No tests configured yet\" && exit 0",
    "bootstrap": "echo 'bootstrap placeholder: install workspace deps'",
    "build": "echo 'build placeholder for monorepo'"
  },
  "devDependencies": {
    "prisma": "^4.0.0",
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^4.0.0",
    "zod": "^3.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
